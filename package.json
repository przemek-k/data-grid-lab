{
  "name": "data-grid-lab",
  "version": "1.0.0",
  "description": "An illustrative example of a reimplementation of React's diffing algorithm in vanilla JavaScript.",
  "main": "index.js",
  "scripts": {
    "dev": "npx tailwindcss -i ./input.css -o ./output.css --watch",
    "build": "npx tailwindcss -o ./input.css -o ./output.css --minify",
    "start": "npx serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Przemyslaw Kalka",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
